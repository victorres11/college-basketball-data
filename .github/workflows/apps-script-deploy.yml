name: Deploy Google Apps Script

on:
  push:
    branches:
      - main
    paths:
      - 'apps-script/**'
      - '.github/workflows/apps-script-deploy.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Authenticate clasp
        run: |
          mkdir -p ~/.config
          # Write clasp credentials to the expected location
          echo "${{ secrets.CLASP_RC }}" > ~/.clasprc.json
          # Also try the config directory location
          mkdir -p ~/.config
          echo "${{ secrets.CLASP_RC }}" > ~/.config/.clasprc.json
          # Verify the file was created
          ls -la ~/.clasprc.json || echo "Warning: ~/.clasprc.json not found"
          cat ~/.clasprc.json | head -3 || echo "Warning: Could not read credentials"
        env:
          CLASP_RC: ${{ secrets.CLASP_RC }}

      - name: Push to Google Apps Script
        working-directory: ./apps-script
        run: |
          # Copy main script to Code.gs (clasp expects Code.gs)
          cp google-apps-script-cbbd.js Code.gs
          
          # Verify clasp can see credentials
          clasp login --status || echo "Note: Login status check"
          
          # Push to Google Apps Script
          clasp push --force
          
          echo "✅ Script pushed to Google Apps Script successfully!"

